## template

> 配置模版渲染器

### API

```js
const templateRender = require('use-template');
templateRender({
  group: 'ascp-comp',
  project: 'test-template',
  version: '5.0.0',
  author: 'kaihao.zkh',
});
```